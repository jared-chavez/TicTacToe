<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TicTacToe"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- =================== ADD THIS ENTIRE SERVICE BLOCK =================== -->
        <service
            android:name=".DataLayerListenerService"
            android:exported="true">
            <intent-filter>
                <!-- This intent filter is crucial. It tells Google Play Services
                     that this service is interested in receiving messages. -->
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <!-- The data element specifies that we only want messages for our app
                     and specifically for the "/game_end_status" path. -->
                <data
                    android:host="*"
                    android:pathPrefix="/game_end_status"
                    android:scheme="wear" />
            </intent-filter>
        </service>
        <!-- ==================================================================== -->

    </application>

</manifest>
